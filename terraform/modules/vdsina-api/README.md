# VDSina API Module

Placeholder for a future Terraform/OpenTofu module or external-data wrapper around the VDSina REST API.

The initial implementation keeps provider mutations in `scripts/vdsina-api.sh` until the API payloads are verified against a disposable server.

