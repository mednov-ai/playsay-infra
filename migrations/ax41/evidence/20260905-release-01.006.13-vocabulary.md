# Vocabulary recovery hotfix — release/01.006.13

## Scope and authorization

The owner explicitly requested production delivery on 2026-09-05, superseding the earlier dev-only constraint. Platform source is `61a1bfdb945da03a06ed474e6eda7c1f25e66c47`, a reviewed isolated patch rebased onto production/develop `219eedf9f60ded72035dd3e705bbc5f0f3bd1da1`. It changes vocabulary web behavior, localized text, regression tests and documentation only. The patch was accepted on dev before the exact commit was fast-forwarded to platform develop and published as `release/01.006.13`.

The hotfix refreshes owner-scoped queries after quick-add, distinguishes unavailable/denied/empty results, preserves undo for the last archived word, adds mutation pending/error/retry states, preserves manual translations against late AI responses, and retries only unsuccessful recipients after a partial group save. Backend, API, schema, learning policy and production feature flags are unchanged.

## Dev acceptance

Jenkins `playsay-web-app-develop` build 301 (`web-codex-hotfix-dev-vocabulary-301`) succeeded from the exact source. All 665 web tests in 131 files passed, as did lint, client generation, production build, SDK/extension checks, the teacher/two-student classroom smoke including reconnect and annotation preservation, and private group homework smoke including autosave, resubmission and foreign assignment denial.

The automatic post-merge develop build 302 also completed successfully from the same source.

Dev web digest: `sha256:bfdb17b30a15fc97b699b23816d2777a7503fdbb3f67d4639865120de236776d`. Runtime annotations matched the exact source and build. All 21 dev ArgoCD applications were Synced/Healthy; all application pods were Running/ready, web had zero restarts. The unrelated email-service pod had 92 historical restarts and was ready; this hotfix does not alter it. Public dev web, Key and canonical issuer discovery returned HTTP 200.

An authenticated disposable student tested real API-backed dashboard recovery, manual translation save without reload, search, edit, favorite, pause/resume, archive of the last word, failed undo followed by Retry, persistence after reload, and desktop/mobile geometry. The only injected failures were browser-local HTTP 503 responses for that student's initial dashboard and one undo request; all successful requests reached the actual dev API. The synthetic entry was archived and the synthetic identity removed through the supported registration service cleanup API. No real user content was modified. Four-locale desktop/mobile UI regression evidence remains in the platform local hotfix evidence document.

## Production gates

Production baseline is `release/01.006.12`, infra candidate `b219b28ae2876a69011a7d3456375dd3c427f211`. Before preparation, all 19 production ArgoCD applications were Synced/Healthy, `sum(livekit_participant_total)=0`, and `min(up)=1`. The four existing protected backup archives at `/var/backups/playsay/release-01.006.10-20260905T113955Z` passed `SHA256SUMS` verification again; restore catalog verification is recorded in the .10 evidence. This web-only change requires no database restore.

The exact release checkout passed the runbook all-workload convergence checker: API gateway, registration, email, AI tutor, vocabulary, keyboard and worksheet-import each reported zero pending changesets. No Liquibase update ran. The launcher cleaned its bounded Jobs/ConfigMaps.

The prepared candidate names only web-app, records the exact accepted source and .12 baseline, and preserves all 18 production values files except the pending web image/build metadata. The inherited API chart changes allow isolated RF routing in dev; exact candidate and .12 production API renders are byte-identical (SHA-256 `859c6f23d7d615a09692c07ad5a68107ff1dd14146a91bda10605c2de2802ad4`). The regional routing preservation guard passed. The current infra helper's complete matrix initially failed because its newer dev RF configuration cannot initialize the older, unchanged release API; this is a cross-revision dev mismatch, not a production render change. All six actual API binding tests then passed with exact candidate production/media-rollback renders and the compatible .12 dev baseline. No API image, source or runtime configuration was changed to bypass that mismatch.

Before promotion, authenticated synthetic API compatibility checks passed on both production origins: create, dashboard inclusion, edit, favorite, pause, archive and restore. Each created entry was archived and the identity removed through the supported cleanup API. The pre-promotion runtime snapshot contained 19 Synced/Healthy .12 applications and 12 Running/ready application pods, all with zero restarts.

## Production result

Jenkins release dispatcher 64 and web build 303 both completed successfully. Ready infra candidate `54ebf57466fb976926cc9d6b4169d6c2a83a880f` records accepted/platform source `61a1bfdb945da03a06ed474e6eda7c1f25e66c47`, baseline .12 and web-app as the sole affected target. Independent final comparison again passed for all 18 production values files and the byte-identical API render. Remote platform develop/release refs still matched the accepted source immediately before promotion. OpenSpec strict validation passed.

Promoted web build: `web-release-01.006.13-303`. Immutable digest: `sha256:ddb0eb6e83199a164f296a965d5296bcbf2e82afb1c4f03e46258b0fe639c3c2`.

The backup checksums were verified again and the authenticated metrics gate reported zero participants/all targets healthy immediately before root promotion; the same metrics guard ran again before web sync. ArgoCD root and web were synced through its authenticated API to the exact ready candidate. All 19 applications became Synced/Healthy at `release/01.006.13`. The prior cloudnative-pg historical operation remained Failed while its sync/health stayed Synced/Healthy; no new failure occurred. All 12 application pods were Running/ready with zero restarts. Only the web pod changed; all other application pod names matched the pre-rollout snapshot. Runtime image and source/build annotations exactly matched the accepted candidate.

Authenticated browser acceptance passed on `https://online.honey.school` and `https://online.honeyschool.ru`: initial dashboard HTTP 503 followed by actual API retry, manual translation save reflected without reload, search, edit, favorite, pause/resume, last-word archive, injected undo HTTP 503 followed by Retry, reload persistence, mobile geometry and no browser runtime errors. Fault injection was confined to the synthetic user's browser; successful operations used the real production API. Entries were archived and the temporary identity removed after both origins passed. An additional complete 390×844 RF mobile flow passed every action, including opening/closing Edit after reload; scrolling kept the bottom card actions above the fixed tools rail. Desktop and mobile screenshots were inspected locally and are not committed.

Both production web origins and both Key origins returned HTTP 200, both API health endpoints reported UP, and canonical issuer discovery matched `https://ops.honey.school/keycloak/realms/playsay`. Direct web bundle SHA-256 is `582423280b5403e3befa8b333e2f491dfba84a0cf51f37f3ee5007f8856c8dac`; RF web bundle SHA-256 is `dc8a7f9813426eb347756d50a52847a7b3c466e231cf2762eae0d4b71ab19c18`. They are byte-equivalent after only the runbook's existing legacy fallback-issuer rewrite at the RF edge. Key bundle hashes remained unchanged from the pre-rollout baseline.

Following acceptance, `argocd-apps/prod/current-release.txt` advances to `release/01.006.13` on infra develop. The release branch remains pinned to the accepted ready candidate; evidence/pointer bookkeeping does not rebuild or modify it.

## Rollback and remaining acceptance

Rollback returns ArgoCD root/web to the ready .12 candidate through the current GitOps procedure, using web digest `sha256:0b7ae06f40132c4d8db7a9e34f1b79894d5ae0e9312b0889383c6f3182d5a5ac`. Recheck zero participants, then verify login, dictionary add/edit/archive/undo, endpoints and application health. Do not rebuild images or restore databases for this web hotfix.

This release accepts the concrete dictionary recovery defects. It does not claim completion of the broader OpenSpec live vocabulary, lesson-recent reconnect, group partial delivery, or vocabulary-specific homework gateway acceptance matrix. Those tasks retain their explicit open status; generic classroom/homework smoke does not replace them.
