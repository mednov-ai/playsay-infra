# RF collaboration route canary

- Candidate platform/infra commits and numeric release:
- Exact UTC start/end and stabilization interval:
- Zero-active-lesson preflight confirmed:
- Production collaboration mode (expected `rf-two-hop`; `off` is rollback only):
- `.school` fresh-token route class:
- `.ru` fresh-token route class:
- Selectel/AX41 nginx and collaboration-service health:
- Privacy validator and log inspection (no token-bearing access record):
- Yjs active min/max, opens, closes by sanitized class, heartbeat terminations:
- Buffered-byte max, drops, forced closes, snapshot retries, service restarts:
- Browser synthetic propagation sample count, p50/p95/max milliseconds, reconnect count:
- LiveKit signaling/media contour (aggregate timestamp correlation only):
- Complete one-lesson and stabilization outcome:
- Acceptance or collaboration-only flag rollback outcome:

Do not record URI/query, addresses, headers, cookies, authorization, tokens, credentials, room/lesson/participant identifiers, drawing coordinates, drawing content, SDP, or ICE candidates.
