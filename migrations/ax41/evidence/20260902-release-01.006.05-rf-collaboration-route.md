# Release 01.006.05 RF collaboration route

## Immutable source

- Platform release: `release/01.006.05` at `62e1a01cf4292002c8870e307480108f00954889`.
- Infra candidate: `release/01.006.05` at `bc3655e` with `status: ready`.
- Jenkins release dispatcher: `#55`, successful.
- Affected images: api-gateway `sha256:2c021b722aafbb4d013db4577bca7be77e1220b4e77593f50236e3e7bee8751b`, web-app `sha256:afc9a120f2fdc47faf74fe08dddb699726d98af1b375ab1c4b763c260f2ba884`, collaboration-service `sha256:42387a0361aad7dfe0f3b6f71878293af3da0aff482cf7d93826658782ae3506`.

## Pre-rollout gates

- LiveKit participants and collaboration connections were both zero before the RF-edge change and again before application promotion.
- The Selectel check-mode diff changed only the exact nginx collaboration route and its reload handler; coturn and UFW remained unchanged.
- RF apply succeeded, and the immediate second check returned `changed=0`.
- Direct and RF `/collab/ws` Upgrade probes without a token both returned `401`.
- All seven declared production Liquibase workloads validated successfully with zero pending changesets; no migration was applied.

## Rollout result

- The production root and all 18 child applications target `release/01.006.05`; all 19 applications are `Synced / Healthy`.
- api-gateway, web-app and collaboration-service run the candidate digests above, are ready and have zero restarts.
- Runtime api-gateway configuration is `PLAYSAY_COLLABORATION_REGIONAL_ROUTING_MODE=rf-two-hop` with `wss://online.honeyschool.ru/collab/ws`; dev remains `off`.
- Runtime collaboration heartbeat is 20 seconds with two missed pongs before termination.
- Direct and RF roots returned HTTP 200, both API health routes returned `UP`, core monitoring targets reported `up=1`, and production restart increase during rollout was zero.
- RF TLS 1.2 negotiated successfully; a TLS 1.3 probe was rejected with the protocol-version alert.

## Remaining owner canary

No fresh classroom token was issued after rollout during operator smoke, so the new route-selection and heartbeat counters correctly had no series yet. Final acceptance still requires one two-participant `.ru` lesson without VPN, drawing propagation samples, a reconnect check, and the matching sanitized metrics window. Roll back only `collaboration.regionalRouting.mode` to `off` if the canary breaches the declared gates.
