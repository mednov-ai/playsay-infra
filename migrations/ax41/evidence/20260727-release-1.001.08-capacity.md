# Production release 1.001.08 capacity desired state — 2026-07-27

## Revisions and artifacts

- Platform release: `release/1.001.08` at `27776832d320eaf38546347ab913b90f1020935a`.
- Ready infra candidate: `release/1.001.08` at `ecdec5cfa58d8740253fb2ae3cab8bcef57b3b00`.
- Web image: `ghcr.io/mednov-ai/playsay-web-app@sha256:b942772e6051753db7ba1a30bbcc5f96af2908b4dc4ffd3a8b1475f6cde884aa`.
- Collaboration image: `ghcr.io/mednov-ai/playsay-collaboration-service@sha256:be50ff7dd5927ae775a57f8d141ea758ecf10532279319dd806d29dc02d8282d`.
- Pre-release logical backup: `/var/backups/playsay/release-1.001.08-20260727T144520Z`.

## Gates completed

- LiveKit reported zero active rooms and participants before host, coturn and workload restarts.
- Application, keyboard and Keycloak custom-format database dumps passed SHA-256 verification and `pg_restore -l`.
- Jenkins built both affected release targets successfully. Its finalizer agent was lost after downstream success; the same committed finalizer ran locally after all 29 CI contract tests passed.
- The finalizer verified the platform branch SHA, affected build commits and immutable digests, unchanged metadata for unaffected workloads, all production target revisions and every Helm render.
- AX41 host sysctl is `rmem_max/wmem_max=16777216`, backlog `10000`, conntrack `524288`, reserved ports `3478,7881,49152-50511`; VM CPU shares are prod/dev/CI `2048/512/512`.
- `playsay-livekit-nat.service` is enabled and active. PREROUTING contains exactly one DNAT rule for TCP `3478/7881`, UDP `3478`, TURN `49152:49999` and LiveKit `50000:50511`; historical and duplicate rules are absent.
- Prod coturn is active with relay range `49152:49999`; a REST-secret authenticated allocation completed successfully.
- All 16 production ArgoCD applications reached `release/1.001.08`, `Synced/Healthy`; no pod remained unready.
- Runtime web and collaboration images matched the candidate digests. Collaboration `/healthz` and Prometheus metrics responded, with zero active rooms/connections.
- VictoriaMetrics reported `up=1` for API, media, collaboration, LiveKit, node exporter, cAdvisor, kube-state-metrics, vmagent, VictoriaMetrics and all five direct/RF blackbox targets.
- Direct and Selectel RF web/API endpoints returned HTTP 200 and API `UP`; canonical OIDC issuer stayed `https://ops.honey.school/keycloak/realms/playsay`.
- Headless Chromium loaded online/key on direct `.school` and RF `.ru` origins without page or request failures. Sign-in redirects used the canonical issuer and the correct callback for each origin.
- Selectel RF TLS 1.2 succeeded and TLS 1.3 was rejected as required.

## Remaining capacity gate

This release installs the desired state but does not certify the numerical target. Run the external two-generator browser test from `docs/capacity-100-lessons.md`, including 100 rooms for 30 minutes and the 120-room recovery step, before declaring 100 concurrent lessons supported.
