# Production release `01.006.01` support-off

Production release `release/01.006.01` installs the atomic RF classroom signaling and media route accepted on dev at platform commit `a8324e2370b9815c8a399d924d213dd620f3430e`. Jenkins dispatcher `50`, API build `api-release-01.006.01-144`, and web build `web-release-01.006.01-275` succeeded. The ready infra candidate is `76f207a10eddaedeee1ff0b2899e8ddb004d0d0f`.

The exact platform release checkout passed the state-based production Liquibase convergence gate for all seven declared workloads with zero pending changesets and no checksum errors. No database update was required.

The operator promoted the production ArgoCD root and affected API/web applications through GitOps. All 19 applications reached `Synced/Healthy` at the exact candidate revision, and every non-completed pod was ready. Runtime API and web images exactly matched `sha256:27fb89ae7fb8d77c5e1051e1b39085a7cdc855fd8cba2a9e9e83bf1abd0d0ae5` and `sha256:60e5a580250771cc3413a333790746fa703627123b7b97c46339d11ec19cbb4c`. Runtime `PLAYSAY_REGIONAL_RELAY_MODE` remained `off`, so new classroom tokens retained the direct signaling/media baseline.

Direct and RF online/keyboard origins returned HTTP 200, both direct and RF API health routes returned `UP`, and Keycloak discovery retained issuer `https://ops.honey.school/keycloak/realms/playsay`. The separate RF-edge reconciliation and later config-on canary remain independently gated.

After separate RF-edge authorization, the exact pushed numeric release passed Ansible check mode with only the expected dedicated nginx `/livekit` route and privacy-safe signaling log change. Apply succeeded, and the immediate second check returned `changed=0`. Nginx and coturn stayed active; the listener, certificate, default-deny firewall, systemd-limit, and loopback-metrics validator passed. Real authenticated TURN allocation and bidirectional-data probes passed over UDP, TCP, and TLS, while invalid credentials were rejected. The direct and RF signaling probes returned the expected bounded unauthenticated response, collaboration Upgrade returned the same authenticated boundary on both contours, and a non-empty 15-second RF sample passed the shared-host gate with maximum CPU `3.419%`, minimum available memory `1,703,899,136` bytes, maximum RX/TX `0.073/0.269 Mbps`, and zero swap, OOM, UDP errors, allocation failures, service restarts, or relay sockets after the probe.

The matching idle production-metrics interval confirmed zero LiveKit participants/joins, zero pod restarts and UDP errors, healthy core/RF targets, and zero collaboration forced closes. It also exposed a fail-closed pre-canary blocker: route-selection counters and idle RTT/jitter/loss histograms had no time series before an authorized room-token/media event, so the strict collector reported those aggregate sources unavailable. Config-on remains blocked until idle-source handling is made explicit without masking target unavailability and the full preflight is rerun.

Follow-up collector hardening on infra `develop` treats only those five absent event aggregates as zero when the independently available LiveKit participant series proves exactly zero participants; it still fails when that source is missing or participants are active. Shell syntax and an authenticated 60-second production query passed with explicit zero route/media values, `core_targets_min_up=1`, `rf_endpoints_min_up=1`, and zero joins, participants, restarts, UDP errors, or collaboration forced closes. This follow-up is not a mutation of the frozen `release/01.006.01` candidate and must be included in the later separately reviewed config-on release.

Rollback changes the production ArgoCD root to `release/01.006.00` and syncs API and web. No database restore is required.
