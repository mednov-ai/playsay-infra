# Production release 01.007.10 evidence

Date: 2026-09-18

Production release `release/01.007.10` adds privacy-safe classroom route observability across the Selectel RF edge, AX41 nginx and the AX41-to-LiveKit path. It also promotes the previously dev-accepted platform changes from commit `2d140f6f767a805b9501fdb0065fe58b42707c9d`.

The final reviewed infra candidate was `ready` at revision `a6341d001fe1f933da0f05045bff410a7c490ec0`. Release dispatcher `82` rebuilt only `game-adapter-service`, passed the `ci-contracts` suite and completed successfully after the runtime correction described below. The final deployed immutable images for the affected application set are:

- API Gateway: `sha256:728e9eef939cdece3270a3fd44e7fd7c562d4889e391dac04f3b9fda1413f4c9`;
- Web: `sha256:887b9da904af59ba046b0b99c141c5ae0d8ee27444b0e2d828c532a938ff1df8`;
- Game adapter: `sha256:15d4a205e63b05cfab97844454a0763a497d290e50577788c0df7f16dc23102e`;
- Collaboration: `sha256:6352387cfe1664b5b2625d98123f7ff88504d883722925c4223b9703da06de88`;
- Keyboard: `sha256:3e5b13bbe699d0d8679d1024d576a1ec5edf723928bb54aa406c5044a99f1d3b`.

The first application sync exposed a release-only game-adapter packaging defect: the new pod failed at startup because `sharp` imported the hoisted `detect-libc` package that was absent from the runtime image. The component was immediately returned through ArgoCD to the healthy `release/01.007.09` digest `sha256:8d8379f5c56a5089c22282ed670804b5fe604792d816657e0183fd7dbd522221`. The runtime image was corrected at platform commit `2d140f6f767a805b9501fdb0065fe58b42707c9d`, a CI regression test was added, dev build `game-adapter-dev-30` reached healthy runtime state, release build `game-adapter-release-01.007.10-31` passed 33 tests, and the corrected production pod became Ready with zero restarts. No database or classroom-data rollback was required.

The production schema-convergence gate was repeated from the final numeric platform checkout. Bounded Liquibase validation and status Jobs for `api-gateway`, `registration-service`, `email-service`, `ai-tutor-service`, `vocabulary-service`, `keyboard-service` and `worksheet-import-service` all reported `0 pending changesets`; no migration or database backup/restore was required.

Immediately before each production application rollout, LiveKit reported zero active participants. All 19 ArgoCD applications reached `release/01.007.10`, `Synced` and `Healthy`; there were no unready pods. Both online origins, both keyboard origins, API health and the production OIDC discovery endpoint returned HTTP `200` after the final sync.

The scoped AX41 and RF observability plays were applied from the numeric release and a second check returned `changed=0` on both hosts. AX41 nginx, RF nginx, coturn and node exporter retained their pre-change `NRestarts=0` and unchanged activation timestamps. The five-second RF timer remained active. VictoriaMetrics received fresh RF-to-AX41 TCP, TLS and HTTP success values of `1`, AX41-to-RF success `1`, AX41-nginx-to-LiveKit success `1`, and no firing route or stale-telemetry alert.

A synthetic WebSocket request through the RF route produced the same opaque request id at RF and AX41 while retaining only the allowlisted status/timing fields. Controlled, non-disruptive probes to unused ports classified an RF-to-AX41 failure as TCP/TLS/HTTP unavailable while the real AX41 nginx-to-LiveKit path remained HTTP `200`; a separate closed AX41-local target failed at connect time while the real RF-to-AX41 phases remained healthy. No real route, firewall, listener, LiveKit process or active lesson was interrupted.

Authenticated teacher/student acceptance remains separate from technical deployment health. No two-participant lesson or full-window regional TURN canary was run in this rollout, so OpenSpec tasks `8.8` through `8.11` remain open.

Application rollback is GitOps-only: return `playsay-prod-root` to `release/01.007.09`, or return only `game-adapter-service` to that release if the defect is isolated to adaptation. Route-observability rollback uses the reviewed scoped Ansible state and must preserve the protected credentials and incident evidence; it must not restart LiveKit or coturn.
