# Production release 01.004.08 evidence

Date: 2026-08-25

Production hotfix `release/01.004.08` keeps platform commit `7a399b13624d00f403cde8573399f59f3c3f7f88` from the accepted `release/01.004.07` runtime and promotes infra candidate `e3a5e8f85ae10a075274342e33f4122933eb3623`. The Keycloak login now renders username/email and password from first paint, makes password submission primary, and starts modal WebAuthn only after the explicit secondary Passkey action. Cancellation and unexpected failure keep both methods usable; page load, locale/theme changes, and password-error reloads do not invoke WebAuthn.

The focused theme regression suite passed zero initialization calls, one request per explicit activation, duplicate-click protection, cancellation, failure, password-switch abort, unsupported WebAuthn, primary/secondary semantics, logical DOM order, and complete Russian, English, German, and French message coverage. Helm dependency resolution, development and production lint/render, and rendered login-theme ConfigMap inspection passed; the downloaded dependency archive was removed. Playwright covered desktop and mobile light/dark layouts, keyboard order, cancellation/failure fallback, and an explicit passkey assertion with a Chromium virtual authenticator.

The first automatic release dispatcher, build `20`, failed closed because a production-based hotfix must not inherit the newer unpromoted `develop` commit. Dispatcher `21` was then run with accepted production commit `7a399b13624d00f403cde8573399f59f3c3f7f88`; it found no platform image targets, preserved every immutable runtime digest, prepared the existing infra hotfix branch, and finalized the candidate as `ready`.

Before promotion, custom-format logical backups were written to `/var/backups/playsay/release-01.004.08-20260825T160408Z`. SHA-256 verification passed for the application, keyboard, and Keycloak archives. PostgreSQL `pg_restore --list` validation found 56, 11, and 100 table-data entries respectively. The hotfix has no Liquibase, SQL, schema, image, or persistent-data change.

After GitOps promotion all 18 ArgoCD applications reached `Synced/Healthy` at the candidate revision. The Keycloak StatefulSet completed its checksum-driven rollout with one ready application pod and zero application-container restarts; all 11 product pods remained ready with zero restarts. Direct and RF online/keyboard routes returned HTTP 200 and discovery retained issuer `https://ops.honey.school/keycloak/realms/playsay`.

Production Playwright smoke covered all four locales in light and dark modes and observed zero `navigator.credentials.get()` calls on initial load. Explicit Passkey activation made exactly one `mediation=optional` call; simulated user cancellation left password and Passkey retry available. A deliberately invalid, non-personal password attempt returned the localized accessible error while keeping the complete password form visible and made zero WebAuthn calls after reload.

Owner-operated phone-only QR and real Touch ID, Face ID, Windows Hello, or equivalent successful credential checks remain required hardware acceptance because automation cannot provide physical user verification. Rollback returns the production ArgoCD root and Keycloak application to `release/01.004.07`, waits for the previous theme checksum rollout, and repeats password plus explicit-Passkey smoke. No database restore is required for this theme-only rollback.
