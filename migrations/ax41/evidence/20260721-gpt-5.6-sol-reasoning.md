# GPT-5.6-sol reasoning rollout evidence

Date: 2026-07-21  
Release: `release/1.001.03`  
Production namespace: `playsay-prod`

This record intentionally contains no API keys, prompts, generated lesson content, personal data, or decoded Secret values.

## Accepted configuration

| Request path | Model | Reasoning effort |
| --- | --- | --- |
| Full material draft | `gpt-5.6-sol` | `high` |
| Accepted-answer suggestions | `gpt-5.6-sol` | `low` |
| HTML-game metadata | `gpt-5.6-sol` | `low` |
| Vocabulary suggestions | `gpt-5.6-sol` | `low` |

The application validates efforts against `none|low|medium|high|xhigh|max`. Compatibility defaults remain `medium` in API Gateway and `none` in Vocabulary Service, while both Helm environments pass all accepted values explicitly. The `playsay-openai` Secret contains only `api-key`; dev and prod retain different keys.

## Git evidence

### Application

- `c18a3bb2b171e894dc81827e25036a6c999e9e31` — explicit reasoning configuration, validation, request bodies, source metadata, and unit tests.
- `fa87820e8a313fce39d9b6adb2d1bd9ec4e6f6b7` — integration of the dedicated Jenkins boundary into `develop`.
- `e0368177d5dca5960ef90af072481e4873a70c7c` — OpenAI strict-schema compatibility fix with backend block-type validation retained.
- `playsay-platform/develop` and `playsay-platform/release/1.001.03` both contain the accepted application revision `e0368177d5dca5960ef90af072481e4873a70c7c`.

### Infrastructure

- `18243b6268a7aced49c0e264fcc45a2c8d06e0b8` — model and reasoning values as Git configuration for dev and prepared prod.
- `d3c394f689028c33bca13acd809a0138b389a02b` — dedicated CI boundary and operational documentation.
- `c05dbeffdce076912416ac6a8610e8cd819aab85` — production digest pins and all 15 ArgoCD targets on `release/1.001.03`.
- Production base and immediate rollback target: `release/1.001.02` at `74904b40c2cc5ad368a08735f21d2feb552e1c2b`.

## Build and static validation

- API Gateway and Vocabulary Service unit tests passed, including all four `reasoning.effort` request assertions and invalid-effort startup rejection.
- The material Structured Outputs schema was submitted directly to the Responses API. The original partial `anyOf` form was rejected as an invalid strict schema; revision `e0368177` removed that incompatible schema construct and retained the same semantic checks in backend validation. The corrected strict schema was accepted by the provider.
- Helm lint and render checks passed for dev and prod. Rendered Deployments expose the model and four efforts as ordinary environment variables, retain only the `api-key` Secret reference, and contain no model Secret reference.
- Jenkins develop builds: API Gateway `#4` (`api-dev-4`) and Vocabulary Service `#2` (`vocabulary-dev-2`) succeeded.
- Jenkins release builds: API Gateway `#5` (`api-release-1.001.03-5`, 1,174,746 ms) and Vocabulary Service `#4` (`vocabulary-release-1.001.03-4`, 442,876 ms) succeeded from application revision `e0368177`.
- An automatic all-module dispatcher created while the release branch first appeared was stopped. Its unrelated AI Tutor/Web/Vocabulary attempts were aborted before publication; only the two selected successful release builds above were promoted.

## Dev acceptance

The change passed two independent dev deployments.

### Develop candidates

- API Gateway `api-dev-4`; Vocabulary Service `vocabulary-dev-2`.
- Full material draft: HTTP 200, about 29 s, provider `openai`, model `gpt-5.6-sol`, effort `high`, valid document.
- Answer suggestions: HTTP 200, about 4 s, at least one valid suggestion.
- HTML-game enrichment: HTTP 202 to `READY`, about 19 s, icon present.
- Vocabulary suggestions: HTTP 200, source `OPENAI`, three valid variants.
- Both ArgoCD Applications were `Synced/Healthy`; pods had zero restarts and no provider-error matches.

### Exact release candidates

- API Gateway: `ghcr.io/mednov-ai/playsay-api-gateway@sha256:3d4440433e01acf46050cf49df110514a616794a4f8616dbe8ce6cdc99ed7782`.
- Vocabulary Service: `ghcr.io/mednov-ai/playsay-vocabulary-service@sha256:eabb5f70b65aaa765604a8bd0d07011826b8be50d87d970b5bacb5a1fb1948b6`.
- Full material draft: HTTP 200, about 34 s, model `gpt-5.6-sol`, effort `high`, valid document.
- Answer suggestions: HTTP 200, about 4 s, valid suggestions.
- HTML-game enrichment: HTTP 202 to `READY`, about 19 s, icon present.
- Vocabulary suggestions: HTTP 200, about 8 s, source `OPENAI`, three valid variants.
- Both Applications were `Synced/Healthy` at infra revision `55ea2fcca539187630d6c51bae6894dc2437ae1d`; exact live image digests matched the candidates and restart counts were zero.
- After confirming that both dev Deployments read only `api-key`, the obsolete `model` field was removed from the dev Secret without decoding or printing the key.

## Production promotion and acceptance

- The production root Application was manually moved from `release/1.001.02` to `release/1.001.03` and synchronized first.
- API Gateway and Vocabulary Service were then synchronized manually and sequentially. Jenkins has no production kubeconfig.
- Production uses the same two exact release digests recorded above. Digests of all other production services were left unchanged.
- Final ArgoCD state: 15 of 15 Applications target `release/1.001.03`; 15 of 15 are `Synced`; 15 of 15 are `Healthy`.
- API Gateway and Vocabulary Service each have `1/1` ready replica and zero restarts.
- Live Deployment configuration reports `gpt-5.6-sol`, draft `high`, and all three lightweight efforts `low`.
- Production `playsay-openai` has exactly one key name: `api-key`.

Authenticated production smoke used a disposable Keycloak ADMIN identity and a private disposable material. Only safe metrics were retained:

| Path | Result | Duration | Validation |
| --- | --- | ---: | --- |
| Full material draft | HTTP 200 | 33.9 s | model and `high` effort recorded in `sourceMeta`; JSON document valid |
| Answer suggestions | HTTP 200 | 5.0 s | `low`; response schema valid; suggestions present |
| HTML-game metadata | HTTP 202 to `READY` | 29 s | `low`; title and icon present |
| Vocabulary suggestions | HTTP 200 | 11.7 s | `low`; source `OPENAI`; variants present |

Post-smoke logs contained zero matches for provider failure, `incomplete_details`, `max_output_tokens`, HTTP 400/401/429, or HTTP 5xx. The disposable material, its database rows, its MinIO objects, its application user, and its Keycloak user were deleted; residue count was zero.

## Rollback

For a critical production error, manually return the root Application and the API Gateway/Vocabulary child Applications to `release/1.001.02`, synchronize in that order, and verify the previous digests and all 15 health states. Any correction must be released as a new numeric patch release; do not mutate `release/1.001.03` after acceptance.
