# Production release 01.003.00 evidence

Date: 2026-08-17

Production release `release/01.003.00` records the owner-accepted `develop` state at platform commit `fdc133188e98f6549d71a3a1d5b9bc711eee2cbb` and infra candidate `d5d618754672309b75634ea2215ec6d5044f3346`. The platform commit is identical to the previous production release, so affected-target detection returned no deploy targets or validation-only suites. The committed CI contract suite still passed 32 tests with three intentional skips. Jenkins webhook router build 81 and release dispatcher build 10 succeeded; the finalizer confirmed unchanged image/build metadata and rendered every production chart before marking the candidate ready. There were no source, Liquibase, SQL, database changelog, image or digest changes.

Before promotion, custom-format logical backups of the application, keyboard and Keycloak databases were written to `/var/backups/playsay/release-01.003.00-20260817T122851Z`. All three archives passed `sha256sum -c` and `pg_restore -l` validation.

The production root and all 17 ArgoCD applications reached `Synced/Healthy` at the exact candidate revision. Because every immutable digest was unchanged, the existing application and Keycloak pods were retained; all remained ready with zero restarts. Public smoke returned HTTP 200 for `online.honey.school`, `key.honey.school`, `online.honeyschool.ru` and `key.honeyschool.ru`, API health `UP`, and canonical discovery issuer `https://ops.honey.school/keycloak/realms/playsay`. The production authorization page contained the Passkey-first heading and action with the password panel available as fallback.

Rollback changes the production ArgoCD root back to `release/01.002.00`. No legacy VPS, legacy application route or protected legacy branch was accessed or changed during this promotion.
