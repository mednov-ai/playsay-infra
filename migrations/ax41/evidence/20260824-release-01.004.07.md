# Production release 01.004.07 evidence

Date: 2026-08-24

Production hotfix `release/01.004.07` promotes platform commit `7a399b13624d00f403cde8573399f59f3c3f7f88` and infra candidate `adff3a5b2dd60463d591127b809ed8e26de3df8e`. Public registration now keeps submit operable while client-side requirements are incomplete, reports deterministic localized validation, enforces the shared password contract in the frontend and backend, allows only current Honey School return origins, and treats repeated confirmation of an already confirmed token as a safe idempotent success.

The platform verification passed frontend lint, 492 web tests, 12 game-sync tests, 11 browser-extension tests, the production web build, the complete registration-service test suite and `bootJar`, affected-target CI validation, Kotlin structure audit, Helm lint/render for development and production, and privacy/diff checks. Playwright covered Russian, English, German, and French on desktop, phone portrait, phone landscape, and forced-colors modes. Invalid keyboard submit sent no request and focused the first invalid control with stable ARIA relationships. Dev deployed web digest `sha256:28ca465281e02e0eae59e736d2366e74a603456957df8d7347fcc373b42502c1` and registration digest `sha256:1c027c2547e77ab1740c4a8dd6205004cc1ccdbe63ca34dd7f5b149035d17108` from the exact platform commit. Disposable-account run `registration-20260824123507` completed registration, email confirmation, repeated confirmation, first OIDC sign-in with `STUDENT`, profile verification, identity cleanup, and mailbox cleanup.

Release dispatcher `19`, web build `web-release-01.004.07-232`, registration build `registration-release-01.004.07-51`, and candidate finalization succeeded. The immutable production images are web `sha256:b55530f58ba55d5e9e8e87a6b35ad65b05923e9798411dae942a5ff20e07170a` and registration `sha256:d9c8135195ea36e64b94d928141fc52076f66ebcc6ba106145ee4b1a4be5281c`.

Before promotion, custom-format logical backups were written to `/var/backups/playsay/release-01.004.07-20260824T125430Z`. SHA-256 verification passed for the application, keyboard, and Keycloak archives. PostgreSQL `pg_restore --list` validation found 56, 11, and 100 table-data entries respectively. There are no Liquibase, SQL, schema, or persistent-data changes in this hotfix.

The first production acceptance request exposed missing `realm-management` role mappings on the existing `playsay-registration-service` service account: Keycloak rejected user lookup and creation with HTTP 403. No synthetic identity remained. Infra commit `3b695eb` added the narrowly scoped, idempotent `configure-keycloak-prod-registration.sh` procedure; it preserved the existing client and credentials, granted only `view-users`, `manage-users`, and `view-realm`, and verified the resulting mappings on two consecutive runs. The registration-owned exact lookup then returned HTTP 200.

Disposable-account runs `registration-20260824130403` on `https://online.honey.school` and `registration-20260824130422` on `https://online.honeyschool.ru` each passed the entire journey, including submit operability, email delivery, confirmation, repeated confirmation, first OIDC sign-in with `STUDENT`, profile verification, identity removal, and mailbox removal. After acceptance all 18 ArgoCD applications were `Synced/Healthy`, no production or Keycloak pod was non-running or unready, production workloads had zero restarts, and the gateway, registration, and email services emitted no warning/error entries during the acceptance window. Both online and keyboard routes returned HTTP 200 and discovery retained canonical issuer `https://ops.honey.school/keycloak/realms/playsay`.

Rollback returns the production ArgoCD root and child applications to `release/01.004.06`. No database restore is required because the release does not change persistent data or schema. Reconcile the prior runtime, then repeat the disposable-account acceptance before declaring rollback complete.
