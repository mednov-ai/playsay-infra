# Production release 01.004.03 evidence

Date: 2026-08-18

Production hotfix `release/01.004.03` promotes platform commit `5624b94e33043eef9991f6fe4c008270a534d144` and infra candidate `87debcc3846c66f641dd94ab340f037e03c27162`. The web-only change restores transparent text annotations over lesson images, enables compact content-driven width and height for new text, and migrates the legacy `240px` fixed-width text shape to smart autosize while preserving deliberately resized annotations. The smoke scripts now default to the current AX41 dev contour; production remains an explicit override.

The isolated release worktree passed 100 Vitest files with 460 tests, ESLint, the TypeScript/Vite production build, smoke-script syntax checks, and `git diff --check`. Jenkins release dispatcher `14`, web build `web-release-01.004.03-215`, and candidate finalization succeeded. The candidate affects only `web-app`; its immutable image is `ghcr.io/mednov-ai/playsay-web-app@sha256:4482d36fba1e7e64909e7c5e2f01ea213d7dceea86ee627b526afe481aaedf48`.

Before promotion, custom-format logical backups were written to `/var/backups/playsay/release-01.004.03-20260818T132533Z`. SHA-256 verification and container-native `pg_restore -l` validation passed for application, keyboard, and Keycloak archives. There are no Liquibase, SQL, or schema changes in this hotfix.

All 18 production ArgoCD applications reached `Synced/Healthy` at the exact candidate revision. The single running web pod uses the candidate digest, is ready, and has zero restarts. Public smoke returned HTTP 200 for `online.honey.school`, `online.honeyschool.ru`, `key.honey.school`, and `key.honeyschool.ru`; API health was `UP`, and discovery retained canonical issuer `https://ops.honey.school/keycloak/realms/playsay`. Chrome also loaded the direct production origin and completed its unauthenticated callback path without a page error. Authenticated annotation smoke was not run in production because the production realm intentionally has no complete demo teacher/two-student smoke cohort and no owner session was available; no production users or credentials were created for the release check.

Rollback changes the production ArgoCD root and `web-app` application back to `release/01.004.02`. No database restore is required because the release does not change persistent data or schema.
