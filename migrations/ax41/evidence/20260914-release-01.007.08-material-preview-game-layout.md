# Production release 01.007.08 evidence

Date: 2026-09-14

Production hotfix `release/01.007.08` restores usable HTML-game sizing in the teacher material authoring preview. The preview now propagates `html-game-focus` to its viewport-owning container and gives the focused game chain a bounded responsive height without changing the HTML-game runtime or remounting the iframe on minimize and reopen.

The source hotfix commit is `69826b28` on `codex/hotfix-material-preview-game-layout`. It was merged into platform `develop` as `1e1d1f6332ebcb42c11434278d82e5e843b984d0`; the numeric platform release points to that exact accepted commit. Targeted local verification passed 10 tests, ESLint, the production frontend build and four external Playwright desktop/mobile geometry checks. The release CI gate passed 142 test files and 752 tests, including the new preview regression.

Jenkins release dispatcher `playsay-platform-dispatch-release` build `78` and web build `327` succeeded. Candidate `release/01.007.08` became `ready` at infra revision `3e01cc53da4a26a611a6680d3982f60f9f6fbf9e` with only `web-app` affected. The deployed image is `ghcr.io/mednov-ai/playsay-web-app@sha256:d9899bf397b619612a6e0deb1472940b4cb2605b9a554312e2bfaab0e505a047`, built as `web-release-01.007.08-327` from platform commit `1e1d1f6332ebcb42c11434278d82e5e843b984d0`.

The production schema-convergence gate ran from the exact platform release checkout. Bounded Liquibase status Jobs for `api-gateway`, `registration-service`, `email-service`, `ai-tutor-service`, `vocabulary-service`, `keyboard-service` and `worksheet-import-service` all validated and reported `0 pending changesets`; no schema update was executed.

ArgoCD switched `playsay-prod-root` to `release/01.007.08` and synchronized `web-app`. All 19 ArgoCD applications reached `Synced/Healthy` at infra revision `3e01cc53da4a26a611a6680d3982f60f9f6fbf9e`. The final web pod was Ready with zero restarts and the expected immutable digest.

Both `https://online.honey.school` and `https://online.honeyschool.ru` returned HTTP `200` for the web application and API health. Headless Chromium loaded both Honey School pages without page errors. Both origins served `assets/app-BVEIy4hl.css`, which contains the new focused material-preview selector and bounded `72vh` viewport rule.

Authenticated teacher acceptance of the exact reported material remains open because no production account was used during operator smoke. It should confirm launch, usable height, reachable minimize control and reopen in the material authoring preview. Deployment and public delivery are complete; this user canary remains the final product-level acceptance check.

Rollback is GitOps-only: return `playsay-prod-root` and `web-app` to `release/01.007.07`. There are no database changes to restore.
