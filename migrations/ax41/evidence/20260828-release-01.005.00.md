# Production release 01.005.00 evidence — 2026-08-28

## Accepted revisions and scope

- Platform numeric branch `release/01.005.00`: `b15c57a619dddd0e7172b7c9b7aea682f7d0e6f0`.
- Accepted infra develop baseline: `7262b76d5df3149fed4af0c839d1f1e8c924d9b8`.
- Disabled-safe infra candidate: `23eed65ed87e5ed8757cc861e87f9f2bd11f3ef0`.
- Staged production commits: database `25fd116247f2694db4ec880ec76024191dccd734`, service `91ac439915b01a8ef5b9e272087b3677e3306f06`, image pull authorization `d5e517d4eddfd4921ac89d6933c4930e29ff864f`, storage endpoint correction `58ef1a7a452c5a60855f2d0002806d1f34ede336`, and gateway `67190abe0cf3c673612c3771040afac3a9225122`.
- Jenkins release dispatcher 39 completed successfully. The worksheet workload is `ghcr.io/mednov-ai/playsay-worksheet-import-service@sha256:c5e2b5df6d12e7d9c0f8443aa55af4663a3309d10e31ff53d834fbecd7ed2966`, build `worksheet-import-release-01.004.13-15`, source `b15c57a619dddd0e7172b7c9b7aea682f7d0e6f0`.

The promoted platform and infra release history was compared chronologically with fetched `origin/develop`. Missing production history was merged without force before the release was frozen. Platform `b15c57a619dddd0e7172b7c9b7aea682f7d0e6f0` and infra `7262b76d5df3149fed4af0c839d1f1e8c924d9b8` were the accepted develop revisions. The release diff included the worksheet-import service, its dedicated database and Liquibase changelog, API Gateway wiring, restricted staging dependencies and disabled-safe feature controls.

## Backups, database and storage

The pre-change backup `/var/backups/playsay/release-01.005.00-20260828T071810Z` passed checksum and archive-list validation: application 434 entries, keyboard 69 and Keycloak 521. `worksheet_import` did not exist at that baseline.

Production provisioning created an environment-local `worksheet_import_app` login, retained `worksheet_import` database, runtime/migration Secret shape, separate service-token Secret, restricted MinIO identity and private `playsay-worksheet-staging` bucket. Metadata/key-shape checks proved no dev or gateway database credential was reused; no secret value was recorded. The bounded migration Job used the accepted changelog and applied one changeset. Verification found three tables, five indexes and eleven constraints. All task-created Jobs, ConfigMaps, provisioning pods, temporary users and port forwards were removed.

The post-enablement backup `/var/backups/playsay/release-01.005.00-enabled-20260828T075355Z` also passed checksum and archive-list validation: application 434 entries, keyboard 69, worksheet import 27 and Keycloak 521.

## Staged rollout and worksheet acceptance

The production root first moved to the disabled-safe candidate. All 19 applications reached `Synced/Healthy`; the worksheet application was healthy without a Deployment. Database/storage provisioning and migration preceded service enablement. The internal service retained `analysis.provider=stub` and the gateway flag remained disabled until internal acceptance.

Two safe-stage defects were detected before public enablement: the private image pull Secret was not referenced, and the production MinIO endpoint named the application namespace instead of the active `storage` namespace. Each was corrected by a separate reviewed GitOps commit. After convergence the worksheet pod was ready with zero restarts, exact immutable digest/source/build metadata, correct Secret references, internal HTTP 200 health and no severe bounded log entries.

The production-local synthetic JPEG/PDF flow used two sources and two pages. Preview output was 100850 bytes, completed in six seconds, allowed manual continuation, and produced idempotent bundle/materialization results. Cancellation cleanup passed, active leases and expired pending sessions were zero, and retention/cleanup settings were `PT72H`/`PT1H`.

Only after internal acceptance was the gateway flag enabled. The public authenticated flow returned create HTTP 201 and session HTTP 200, processed two sources/two pages, produced the same 100850-byte preview in three seconds and materialized idempotently. Missing authentication returned 401 and a roleless user returned 403. Temporary synthetic identities were removed. Final database state contained one migration changeset, two materialized synthetic sessions, zero active leases and zero expired pending sessions.

## Production acceptance

- Direct and RF online, keyboard and API health endpoints returned HTTP 200; the API reported healthy.
- OIDC discovery returned 200 with canonical issuer `https://ops.honey.school/keycloak/realms/playsay`.
- RF TLS 1.2 succeeded and TLS 1.3 was rejected as designed.
- Browser smoke covered both `online` and `key` on `.honey.school` and `.honeyschool.ru`: password/passkey entry methods were visible, authenticated `/api/me` returned 200 on online origins, and there were no page errors.
- TCP reachability to production realtime ports 3478 and 7881 succeeded.
- Every one of the 19 ArgoCD applications targeted `release/01.005.00` and was `Synced/Healthy`; affected pods were ready with zero restarts. CloudNativePG database resources for keyboard and worksheet import reported `applied=true`.
- In the clean observation window beginning `2026-08-28T08:05:04Z`, API Gateway and worksheet service produced zero severe log entries and production emitted zero Warning events.

Two earlier API Gateway unique-subject errors were caused by simultaneous first `/api/me` calls for raw temporary Keycloak smoke users that bypassed the normal registration workflow. They contained no production learner data, did not recur in the clean observation window and the temporary users were removed. Earlier worksheet image-pull/readiness warnings corresponded to the two safe-stage fixes above and did not recur after convergence.

Actual passkey credential creation and destructive lesson/link mutations were not repeated on production because the platform tree and immutable digests are unchanged from accepted `release/01.004.13`. Their 2026-08-26 acceptance evidence is inherited; this release repeated password entry, method visibility, authenticated identity, all public origins, keyboard shells, realtime reachability, migrations and the complete new worksheet journey.

## Rollback

1. Commit the API Gateway worksheet flag to disabled on `release/01.005.00` and wait for ArgoCD convergence.
2. Commit the worksheet service flag to disabled and wait until its Deployment is removed; keep TTL cleanup obligations accounted for.
3. Return the production root to `release/01.004.13` through the reviewed GitOps procedure.
4. Retain the `worksheet_import` database/role, staging objects and material source attachments. Do not delete or manually migrate them during rollback.
5. Use a verified logical backup only after a separately approved incompatible-schema or data-recovery decision.
