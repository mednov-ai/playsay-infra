# Release 01.006.08 regional media restoration

## Approved scope and source

Owner explicitly authorized commit/push, reconciliation into both develop branches and production deployment after lessons ended. Initial fresh check found zero participants and all 19 production ArgoCD applications Synced/Healthy on `release/01.006.07`.

Platform `541bc558539e804ee597e044e9f87d0f977a83dc` preserves all `.07` code plus the restored regional routing changes; it includes the previous develop merge and is pushed to develop. Relative to `.07`, only API/web and CI verification are affected. Infra reconciliation preserves `.07` images and unrelated configuration; the intended production runtime setting change is media `off` → `rf-turn-relay`, with signaling and collaboration both retained at `rf-two-hop`. Dev remains direct/off. No VM, DNS, secret rotation or host/service restart is included.

## Preflight

- Existing Selectel coturn passed configuration, invalid-credential rejection and UDP/TCP/TLS allocation/data diagnostics during the authorized earlier zero-participant check. These are infrastructure probes, not browser-to-LiveKit proof.
- A protected API/Selectel REST-secret comparison returned only `match`; values were not printed or persisted.
- Selectel-to-AX41 TCP connectivity passed. Production UDP forwarding and DNAT rules exist; the allow precedes blanket rejection.
- Corrected RF collector repeated cleanly at 09:18:41–09:19:02 UTC: max CPU 1.674%, min available memory 1,722,802,176 bytes, RX/TX maxima 0.058/0.143 Mbps, zero allocations/failures/auth failures/swap/OOM/UDP errors/service restarts, both services up. The preceding sample with Broken pipe warnings is not accepted evidence.
- Production idle aggregates repeated without parsing errors after collector correction: participants and collaboration connections zero, core and RF endpoint targets up, no pod restarts or UDP errors. Snapshot retries report zero only because the snapshot histogram is present with saved observations; missing entire sources remain errors.
- All seven exact `.08` changelog sets passed the production status-only schema checker with zero pending changesets. No migration update was executed.
- Rendered prod/dev/media-rollback settings passed actual application-binding tests after develop reconciliation. Legacy `.07` source is rejected by the new source/config guard.

## Delivery status

Dev dispatcher #173, API #151 and web #284 completed successfully. API and web ArgoCD applications are Synced/Healthy with one ready replica each. Dev API digest is `sha256:cc428ae9c66288ba0c7a3db1bdeee49f608165e2d07942b6b5d257f9bd7b7633`; web digest is `sha256:06b4ff8512b12c6014406a1058d704e6301e8bea84df961c59e41031f0df0bd3`. Automated dev image commits are reconciled locally through infra `97467bf`.

Platform numeric branch `release/01.006.08` was pushed at the same dev-accepted source `541bc558539e804ee597e044e9f87d0f977a83dc`; release dispatcher #59 is running. Immutable candidate and promotion evidence will be filled after completion. This document is not a statement of production deployment or no-VPN acceptance.

## Acceptance and rollback

After ready candidate promotion, verify runtime source/digests/flags and both public origins. Obtain fresh authorized room tokens, test the exact returned TURN credential, and correlate both no-VPN browsers' relay evidence with nonzero Selectel allocations for camera/audio/screenshare. The full existing one-lesson quality/resource window remains required.

If canary fails, make a reviewed media-only off config candidate with `regionalRoutingReview: media-only-rollback`, preserve healthy signaling/collaboration and allow allocations to drain. Do not restart nginx/LiveKit or restore databases for this routing-only change.
