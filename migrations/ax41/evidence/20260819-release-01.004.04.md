# Production release 01.004.04 evidence

Date: 2026-08-19

Production hotfix `release/01.004.04` promotes platform commit `54930b91785aa2998089e876aea144a509dd676e` and infra candidate `442c99474f968268120e130472b2646b17fe1f80`. The web-only change starts the Clipboard API write during the teacher's click and resolves the generated participant-link payload asynchronously, preserving Chrome's transient clipboard permission and avoiding the page-jumping prompt fallback in the normal supported path.

The isolated production worktree passed the focused schedule test suite with 8 tests, full web-app ESLint, the game-sync build and the TypeScript/Vite production build. Dev build `web-hotfix-lesson-link-clipboard-217` deployed the exact commit and passed its Jenkins UI smoke. The commit was then fast-forwarded to `develop`; dispatcher `120` and `web-dev-218` passed the complete affected-target pipeline on the same commit. The initial malformed manual request `216` was aborted before a valid checkout and did not update Git or GitOps state.

Release dispatcher `15`, web build `web-release-01.004.04-219` and candidate finalization succeeded. The candidate affects only `web-app`; its immutable production image is `ghcr.io/mednov-ai/playsay-web-app@sha256:6fff0428faa45e3c4f89ad591605dbf984e05c62bd48dbaf356302f2359eaede`. The candidate records `acceptedDevCommit` and `platformCommit` as the same exact hotfix commit.

Before promotion, custom-format logical backups were written to `/var/backups/playsay/release-01.004.04-20260819T123355Z`. SHA-256 verification passed for the application, keyboard and Keycloak archives. PostgreSQL 17 `pg_restore --list` validation found 56, 11 and 100 table-data entries respectively. There are no Liquibase, SQL, schema or persistent-data changes in this hotfix.

The production root and all 18 ArgoCD applications reached `Synced/Healthy` at the exact candidate revision. The single running web pod uses the candidate digest, is ready and has zero restarts. Public smoke returned HTTP 200 for `online.honey.school`, `online.honeyschool.ru`, `key.honey.school` and `key.honeyschool.ru`; API health was `UP`, and discovery retained canonical issuer `https://ops.honey.school/keycloak/realms/playsay`. Chrome completed the unauthenticated production callback path, returned to the localized Honey School sign-in screen and emitted no page errors. The focused clipboard regression is covered by the test that resolves a delayed participant-link request into the ClipboardItem payload without calling the expired `writeText` fallback.

Rollback changes the production ArgoCD root and `web-app` application back to `release/01.004.03`. No database restore is required because the release does not change persistent data or schema.
