# Production release 01.002.00 evidence

Date: 2026-08-17

Production release `release/01.002.00` promotes platform commit `fdc133188e98f6549d71a3a1d5b9bc711eee2cbb` and the ready infra candidate `73f26533578e4a535e3c07e6050fb4b52b79a412`. Jenkins release dispatcher build 9 completed successfully; all 12 affected module builds and the finalizer succeeded. The candidate recorded the accepted dev commit equal to the release platform commit. There were no Liquibase, SQL or application database changelog changes.

Before promotion, logical backups of the application, keyboard and Keycloak databases were written to `/var/backups/playsay/release-01.002.00-20260817T114219Z`. All three archives passed `sha256sum -c` and `pg_restore -l` validation. The existing Keycloak PostgreSQL StatefulSet, PVC and PostgreSQL image were preserved.

The production root and all 17 ArgoCD applications reached `Synced/Healthy` at the candidate revision. All product deployments were ready and available with zero restarts on the newly created application pods. Keycloak started successfully as version `26.7.1` from `quay.io/keycloak/keycloak@sha256:f1f1f01e472c8a78df40d8f2a49a925274eda4d3d80d5f6edbb5c880ee3c01c6`; its passwordless WebAuthn policy was reconciled idempotently with RP ID `ops.honey.school` through the production guest's local NodePort because the public edge intentionally hides master/admin endpoints.

The owner accepted real-device passkey registration and repeat login on mobile Safari before promotion. Post-promotion smoke confirmed HTTP 200 for `online.honey.school`, `key.honey.school`, `online.honeyschool.ru` and `key.honeyschool.ru`, API health `UP`, and canonical discovery issuer `https://ops.honey.school/keycloak/realms/playsay`. Browser smoke confirmed that the production login is Passkey-first, attempts the native picker once, keeps password fields collapsed until the fallback action is selected, focuses the username after selection, and emits no console errors.

Rollback returns the production ArgoCD root to `release/1.001.09`. If rollback follows a Keycloak schema failure, scale Keycloak to zero, restore the verified `keycloak.dump`, return GitOps to the previous release and only then start the previous Keycloak image; image-only rollback after schema migration is not accepted. No legacy VPS or legacy branch was accessed or changed during this rollout.
