# AX41 host, VPN and Cockpit evidence — 2026-07-20

Target: `65.109.55.110` (Hetzner AX41, Ubuntu 24.04).

## Storage health

- `/dev/md0`, `/dev/md1` and `/dev/md2` are RAID1, `clean`, `2/2` active/working, `0` failed; `/proc/mdstat` reported `[UU]` for every array and no resync in progress.
- Root filesystem: `/dev/md2`, ext4, 436 GiB total, 410 GiB available at capture time.
- `/dev/nvme0`: SMART `PASSED`, critical warning `0x00`, 19% used, zero media/data-integrity errors and zero error-log entries.
- `/dev/nvme1`: SMART `PASSED`, critical warning `0x00`, 37% used, zero media/data-integrity errors and zero error-log entries.

## Automated host baseline

- The AX41 Ansible playbook installed and verified KVM/QEMU/libvirt, hardware acceleration, SMART monitoring, OpenTofu 1.12.4, WireGuard, UFW and Cockpit Machines.
- A second complete playbook run finished with `changed=0`, `failed=0`.
- `libvirtd`, `wg-quick@wg0` and `cockpit.socket` were active after convergence.
- OpenTofu roots `platform`, `dev` and `prod` passed `tofu init -backend=false` and `tofu validate`; provider lock files pin `dmacvicar/libvirt` 0.9.8.

## VPN and firewall gate

- WireGuard server address: `10.250.0.1/24`, public endpoint `65.109.55.110:51820/udp`.
- Permanent peers: MacBook `10.250.0.2/32` and phone `10.250.0.3/32`. Private keys and ready-to-import profiles remain outside Git under `/Users/evgeniymednov/Backups/PlayAndSay/wireguard/`.
- A one-time peer on the retiring VPS established a real Internet WireGuard handshake and reached `10.250.0.1` with 3/3 ICMP replies and 0% loss. After UFW activation, a second temporary tunnel returned HTTP 200 from `https://10.250.0.1:9090/`. The temporary peer and its key were removed immediately.
- UFW is active with default deny incoming/routed and public allows only for TCP 22/80/443 plus UDP 51820. Authenticated `wg0` traffic is allowed; forwarding is limited to `wg0` and the planned `virbr60` workload bridge.
- Cockpit listens only on `10.250.0.1:9090`; a public TCP connection to `65.109.55.110:9090` timed out while public SSH remained reachable.
- Named Cockpit administrator `playsay` has a generated password stored in the local macOS Keychain item `PlayAndSay AX41 Cockpit`. The password is not in Git or this evidence.

## Patch and reboot gate

- Ubuntu security updates and kernel `6.8.0-136-generic` were installed, followed by a controlled reboot.
- The first reboot check exposed an ordering bug: a socket bound to the WireGuard address could start before `wg0`. A direct socket-to-WireGuard dependency also created a systemd ordering cycle. The Ansible role was corrected to use `playsay-cockpit-vpn.service`, a late oneshot starter ordered after `wg-quick@wg0`, while `cockpit.socket` contains only the VPN-address binding.
- After the corrected reboot, all RAID arrays returned `[UU]`; SSH, libvirt, WireGuard, UFW, the VPN-gated Cockpit starter and `cockpit.socket` were active. No systemd ordering cycle remained, and Cockpit listened only on `10.250.0.1:9090`.
- A fresh external tunnel after reboot received HTTP 200 from Cockpit; a connection to public `65.109.55.110:9090` timed out. The final complete Ansible run again reported `changed=0`, `failed=0`.

## Remaining human gate

Import and activate the actual MacBook and phone profiles, then confirm a handshake from each carrier/private-NAT connection. Public SSH remains enabled until that two-client gate and post-reboot recovery check pass.

During installation of the temporary WireGuard diagnostic client on the retiring VPS, the host package manager restarted k3s. k3s returned `Ready`; Docker and the Amnezia interface stayed active, and both `https://online.play-and-say.ru/` and `https://play-and-say.ru/` returned HTTP 200 immediately afterward.
