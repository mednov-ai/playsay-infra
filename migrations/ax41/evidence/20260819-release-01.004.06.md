# Production release 01.004.06 evidence

Date: 2026-08-19

Production hotfix `release/01.004.06` promotes platform commit `b53288c18f92dbd8e7033eebb61544ee902bc510` and infra candidate `d9d6c8981e491b8f08a8665beea1046f39b43687`. Lesson links now default to `https://online.honeyschool.ru`, let a production teacher explicitly choose `https://online.honey.school`, and keep both choices on the single safe `https://dev.online.honey.school` origin in dev. The backend uses a fail-closed origin enum for generated participant links and managed-student `continueUrl` values. The schedule action menu no longer uses native details/summary focus behavior, opens upward near the viewport edge, preserves focus without scrolling, and falls back to an in-app manual-copy dialog instead of `window.prompt`.

The isolated platform worktree passed the full api-gateway tests, frontend generation, lint, all 463 frontend tests, production build, OpenAPI generation, 35 CI contract checks with three expected skips, Helm lint/render for dev and prod, and `git diff --check`. Focused browser tests cover both production origin choices, delayed ClipboardItem permission, fallback behavior, keyboard navigation, and scroll preservation.

Dev topic builds `api-codex-lesson-link-origin-hotfix-109` and `web-codex-lesson-link-origin-hotfix-221` deployed the exact commit and reached `Synced/Healthy`. Authenticated Chromium acceptance created a temporary lesson for `student-demo`, copied an actual `https://dev.online.honey.school` participant URL through `navigator.clipboard`, opened it as the test student at the exact classroom path, emitted no JavaScript dialog, and cleaned the lesson. A second browser pass at non-zero scroll kept `scrollY` at `180 -> 180`. Consuming the managed-student invite changed the demo credential as designed; the idempotent Keycloak dev configurator restored the secret-backed demo password and resynchronized Jenkins. The first develop web build exhausted its login retries before that repair; the complete replacement build `web-dev-223` passed all checks and UI smoke. Develop API build `api-dev-110` also succeeded, and both running dev images record the exact platform commit.

Release dispatcher `17`, API build `api-release-01.004.06-111`, web build `web-release-01.004.06-224`, and candidate finalization succeeded. The immutable production images are API `sha256:4d4dbed2ca8bd7249c28fd981f8489ac6b097fff4d595b578eac4ddff5b6fe21` and web `sha256:60318c61f140447c291e234fcb574205396dc810f4e2fff23190f4b79b49adfd`. The ready candidate records `acceptedDevCommit` and `platformCommit` as the same exact hotfix commit and affects only `api-gateway` and `web-app`.

Before promotion, custom-format logical backups were written to `/var/backups/playsay/release-01.004.06-20260819T151821Z`. SHA-256 verification passed for the application, keyboard, and Keycloak archives. PostgreSQL 17 `pg_restore --list` validation found 56, 11, and 100 table-data entries respectively. There are no Liquibase, SQL, schema, or persistent-data changes in this hotfix.

The production root and all 18 ArgoCD applications reached `Synced/Healthy` at the exact candidate revision. The running API and web pods use the candidate digests, are ready, and have zero restarts; no production pod remained unready. Runtime API configuration contains canonical `https://online.honey.school` and RF-default `https://online.honeyschool.ru` origins. Public smoke returned HTTP 200 for both online and both keyboard origins, API health was `UP`, and discovery retained canonical issuer `https://ops.honey.school/keycloak/realms/playsay`. Headless Chromium loaded both production online origins without page or request errors; each sign-in flow reached the canonical issuer and retained its own application origin in `redirect_uri`.

Rollback changes the production ArgoCD root and all child applications back to `release/01.004.05`. No database restore is required because the release does not change persistent data or schema.
