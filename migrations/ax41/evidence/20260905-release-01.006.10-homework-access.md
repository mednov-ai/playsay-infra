# Production hotfix `01.006.10`: teacher homework access

## Scope and source

The owner authorized the homework-access hotfix, its merge into `develop`, and production delivery. Platform hotfix commit `78cfb99a` scopes a non-owner teacher's assignment view to students they currently manage, including active delegates, while keeping owner and administrator visibility unchanged. It also applies the same scope to recipients, submissions, progress, aggregates, and exact vocabulary-review access. The hotfix was merged into platform `develop` as `aec357ce650cd6ed09e03e40267109256379652b` and the fixed-width release branch `release/01.006.10` points to that exact commit.

Production previously ran `release/01.006.08`; the separately built `.09` candidate had not been promoted. Relative to the running release, `.10` therefore includes the already dev-accepted web self-capture fix from `.09` as well as this API hotfix. Jenkins correctly routed only `api-gateway` and `web-app`.

## Verification and delivery

- Focused mixed-assignment regression tests cover primary, delegated, second and unrelated teachers, revocation narrowing, scoped aggregates, exact submission access, and vocabulary review. The complete API Gateway check, OpenAPI export/client generation, and frontend workspace build passed locally.
- Dev dispatcher `#176`, API build `api-dev-153`, and web build `web-dev-291` completed successfully for the accepted source commit and rolled out through dev GitOps.
- Production release dispatcher `#61`, API build `api-release-01.006.10-154`, and web build `web-release-01.006.10-292` completed successfully. Jenkins finalized infra candidate `3fe42caec6293df70e7d6270191b66c3501268a6` with `status: ready`, `acceptedDevCommit` and `platformCommit` both equal to `aec357ce650cd6ed09e03e40267109256379652b`.
- The immutable API digest is `sha256:c414c7e48c98ff6ff4ee5b4fd48fc15aa9e64b20fcf9a9fa285fb952e1c3a387`; the immutable web digest is `sha256:5cdfafc70ae4f92fd3248d7b9bdbd711b67ab69d0ca34b5c47dcde81fcbf0d5c`.
- Protected custom-format backups for the application, keyboard, worksheet-import, and Keycloak databases are stored under `/var/backups/playsay/release-01.006.10-20260905T113955Z`. SHA-256 verification and `pg_restore --list` passed for all four archives.
- The exact release checkout passed the state-based production Liquibase convergence gate for all seven declared workloads with zero pending changesets; no migration update was executed.

Immediately before promotion, all 19 production ArgoCD applications were `Synced/Healthy`, no pod was unready, and `livekit_participant_total` was zero. The operator switched the production root to the exact ready candidate and synced only `api-gateway` and `web-app`. Both deployments reached one updated/ready replica with zero failed ArgoCD applications; their running images and source annotations match the candidate.

Post-rollout checks passed on both `https://online.honey.school` and `https://online.honeyschool.ru`: web returned HTTP 200 and `/api/actuator/health` returned `UP`. Keycloak discovery retained issuer `https://ops.honey.school/keycloak/realms/playsay`. All 19 ArgoCD applications remained `Synced/Healthy` at the candidate revision and LiveKit participants remained zero.

## Rollback

If assignment access regresses, return the production ArgoCD root to `release/01.006.08` and sync `api-gateway` and `web-app`. No database restore is required because this release contains no schema change. Preserve the verified backup unless a separately authorized data-recovery decision requires it.
