# Production release 01.004.05 evidence

Date: 2026-08-19

Production hotfix `release/01.004.05` promotes platform commit `711eea8d4740b9c0672cecd4be6b52f0e75ef597` and infra candidate `3d7e8bb407508ba1a5b7cc04ab02e6b613ee5151`. The follow-up fixes the registration-service return URL allowlist for the current dev and production Honey School application origins and removes the current runtime dependency on the protected legacy `*.play-and-say.ru` origins. Before the fix, managed-student participant link generation failed with `400 INVALID_REQUEST` before the frontend could write to the clipboard.

The isolated platform worktree passed the focused return URL policy and registration controller tests, the full `registration-service` test task, `bootJar`, package-structure audit and `git diff --check`. Dev build `registration-hotfix-lesson-link-return-to-43` deployed the exact commit and reached `Synced/Healthy`. The same commit was then fast-forwarded to `develop`; dispatcher `121` and `registration-dev-44` completed successfully. The initial malformed manual request `42` was aborted during checkout before a valid source revision and did not update Git or GitOps state.

Authenticated browser acceptance used `teacher-demo` and a temporary future lesson for `student-demo`. The participant-link action copied one `https://dev.online.honey.school/join#…` URL into the browser clipboard, kept page scroll at `180 -> 180`, opened no JavaScript prompt, emitted no page error and showed `Lesson links copied`. The temporary lesson was deleted after acceptance and the API returned `204`.

Release dispatcher `16`, registration-service build `registration-release-01.004.05-45` and candidate finalization succeeded. The candidate affects only `registration-service`; its immutable production image is `ghcr.io/mednov-ai/playsay-registration-service@sha256:38ee30c5f799480df11f829cfcc5feba747a4ce67512bd32c9009ed774c8f506`. The candidate records `acceptedDevCommit` and `platformCommit` as the same exact hotfix commit.

Before promotion, custom-format logical backups were written to `/var/backups/playsay/release-01.004.05-20260819T134233Z`. SHA-256 verification passed for the application, keyboard and Keycloak archives. PostgreSQL 17 `pg_restore --list` validation found 56, 11 and 100 table-data entries respectively. There are no Liquibase, SQL, schema or persistent-data changes in this hotfix.

The production root and all 18 ArgoCD applications reached `Synced/Healthy` at the exact candidate revision. The running registration-service pod uses the candidate digest, is ready and has zero restarts; no production pod remained unready. Public smoke returned HTTP 200 for `online.honey.school`, `online.honeyschool.ru`, `key.honey.school` and `key.honeyschool.ru`; API health was `UP`, and discovery retained canonical issuer `https://ops.honey.school/keycloak/realms/playsay`. Browser smoke loaded the production Honey School sign-in surface without console errors.

Rollback changes the production ArgoCD root and `registration-service` application back to `release/01.004.04`. No database restore is required because the release does not change persistent data or schema.
