# Production hotfix 01.007.02: mobile dice delivery

The owner requested immediate production delivery and reconciliation into platform `develop`. Platform commit `7081687f327964c535edda5a44e2df6ec5bb89ad` replaces the checkbox-like empty dice mark with two dotted dice and makes a roll request observable: one click creates one request, disables duplicate submission while pending, accepts only the matching lesson/request result, reports unavailable or unconfirmed delivery, and never retries automatically.

## Validation and candidate

- The isolated release checkout passed frontend lint, the production build, 139 web test files with 735 tests, 12 game-sync SDK tests and 12 browser-extension tests.
- Dev Jenkins published `web-dev-316` from the exact accepted commit. ArgoCD reconciled `web-app` to infra `cede31a1f44596d10e2a77a44648c672cae40900`, Synced/Healthy on immutable digest `sha256:d2b1b348041e0e5d9c8cc7529f34372bd04dd5efa840e1da1373164884ac8d97`.
- Release dispatcher 72 rebuilt only `web-app`. Ready infra candidate `a8a26aba083502c4f85f6a7d2ab234745cdd9514` records the exact platform source and immutable production digest `sha256:08eb9eec28c09df9c30d6b9d8444ee8f7f8a93445b91b2ae704ad354ef73a7c4`.
- The retained application backup `/var/backups/playsay/release-01.007.00-20260907T161638Z/playsay.dump` was rechecked at SHA-256 `7a0c160c9821519fb5e902c24a8d92fbef4b6fd369f2b5c1c31f5b2f1354f4e7`; its restore catalog was accepted in the 01.007.00 evidence. This web-only release has no database or schema change.
- The exact release checkout passed the required state-based Liquibase convergence check for all seven declared production workloads with zero pending changesets.

## Promotion and verification

Immediately before root promotion and again before the web sync, two checks ten seconds apart reported zero LiveKit participants and zero lessons in the active access window. The authenticated ArgoCD API moved `playsay-prod-root` to the ready candidate and synced only `web-app`; no workload apply, database update, Secret change, edge change or unrelated service restart was performed.

All 19 production applications reached Synced/Healthy on `release/01.007.02`. The web deployment reached one updated/ready replica with zero unavailable replicas and the exact candidate digest. All 12 production pods are ready with zero restarts. Both online origins returned HTTP 200, both API health endpoints returned `UP`, and OIDC discovery retained issuer `https://ops.honey.school/keycloak/realms/playsay`. Fresh Chromium loads on the direct and Russian origins reported zero page errors. The production bundle contains the two-dice initial mark and the Russian pending, unavailable and unconfirmed delivery messages.

The component and delivery paths were exercised in WebKit iPhone emulation before release. A physical iPhone Safari roll was not performed from the operator workspace and remains an owner acceptance check. Rollback returns the root and `web-app` to `release/01.007.01` after a fresh zero-participant check; no schema rollback or database restore is required.
