# Production release 01.007.09 evidence

Date: 2026-09-15

Production hotfix `release/01.007.09` adds bounded recovery for expired classroom media credentials and clarifies the identity-confirmation stage for a learner opening a reusable lesson link. An expired, invalid or near-expiry media route now causes one authorized room-token refresh and provider replacement while preserving the active lesson context. A matching active learner session still enters automatically; an accepted link without sufficient identity proof now presents explicit email, Lobby and account-switch recovery choices.

Platform commit `2a455ef4e206605006a910ffad82d03e5f6d902b` was created from production release `01.007.08` and is the exact head of platform `develop` and numeric branch `release/01.007.09`. Infra `develop` contains the classroom recovery runbook as commit `e250d321942abd7d6a66742483d610c1b68c9a0b`. The original dirty platform and infra checkouts were not used for release operations.

Dev dispatcher `202` completed successfully. API Gateway build `api-dev-183` and Web build `web-dev-328` deployed immutable digests from the exact source commit, reached `Synced/Healthy`, and returned HTTP `200` from the public dev web and health endpoints.

Production release dispatcher `79`, API Gateway build `184` and Web build `329` completed successfully. The Jenkins finalizer marked candidate `release/01.007.09` ready at infra revision `f49d9f91d1c65472a0e2b55662d39d89149efa80`, affecting only `api-gateway` and `web-app`. The deployed images are:

- `ghcr.io/mednov-ai/playsay-api-gateway@sha256:38289202208a5d50e3edf451807de2e2ad9fd8808fb22773b10ccec8f9a6171c`;
- `ghcr.io/mednov-ai/playsay-web-app@sha256:b8be38e3ba494cb7766555ed689e2e455276c95cc359e1c9294fdb81345adb42`.

The production schema-convergence gate ran from the exact numeric platform release checkout. Bounded Liquibase validation and status Jobs for `api-gateway`, `registration-service`, `email-service`, `ai-tutor-service`, `vocabulary-service`, `keyboard-service` and `worksheet-import-service` all reported `0 pending changesets`; no migration or database restore was performed.

Immediately before promotion, LiveKit reported zero active rooms and zero active participants. ArgoCD promoted `playsay-prod-root`, API Gateway and Web through the reviewed GitOps path. All 19 applications reached `Synced/Healthy` at infra revision `f49d9f91d1c65472a0e2b55662d39d89149efa80`; the API Gateway and Web pods were Ready with zero restarts and matched the reviewed immutable digests and source commit.

Both `https://online.honey.school` and `https://online.honeyschool.ru` returned HTTP `200` for the web application and API health. External headless Chromium loaded both Honey School pages with a visible body, no page errors and no failed requests.

Authenticated acceptance remains separate from deployment health. It still requires an assigned learner link and two authorized participant sessions to prove both confirmation paths and a controlled post-expiry media interruption without retaining identity, token, room, address, candidate, SDP or lesson content in evidence.

Rollback is GitOps-only: return `playsay-prod-root`, API Gateway and Web to `release/01.007.08`. No database restore, coturn/LiveKit restart, credential rotation or network change is required.
