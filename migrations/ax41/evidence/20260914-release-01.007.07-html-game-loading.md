# Production release 01.007.07 evidence

Date: 2026-09-14

Production hotfix `release/01.007.07` restores HTML-game uploads through both external production edges and replaces unbounded game loading with terminal unavailable, retry and close states. Platform `develop` and the numeric release contain source commit `99ca4f960be0b81e9a95e42f9adc2b9a1f3151c3`. Infra `develop` contains the edge reconciliation and the production-block detection correction through `116a6da`.

Jenkins release dispatcher `playsay-platform-dispatch-release` build `77` and web build `325` succeeded. The deployed image is `ghcr.io/mednov-ai/playsay-web-app@sha256:ec8236d9ab2c5f79e5648cd07c1811e20b0dc4719758654b05be09c7939993e7`, built as `web-release-01.007.07-325` from the exact platform commit above. Jenkins finalized the original candidate successfully. A subsequent Ansible-only correction changed only the AX41 production-route detector and its regression test; an exact diff proved that Helm charts and ArgoCD application manifests remained unchanged before the candidate was restored to `ready` at infra commit `fd95bbdfbb1b1d5468beca23b561fa0567522a1c`.

The bounded edge play added only the `21m` HTML-game upload location and graceful nginx reload on the Selectel RF edge. A production-only detection fix then added the same location to the AX41 `online.honey.school` TLS server with upstream `10.60.0.20:32083`; it did not mistake the existing `dev.online.honey.school` route for production. `nginx -t` passed on both edges and repeated Ansible checks reported `changed=0`.

Before application promotion, multipart requests containing the supplied 3,767,254-byte and 5,549,030-byte HTML games reached the application through both `https://online.honey.school` and `https://online.honeyschool.ru`: unauthenticated probes returned application HTTP `401`, never nginx `413`. These probes are transport evidence and do not persist materials.

The production schema-convergence gate ran from the exact platform release checkout. Bounded Liquibase status Jobs for `api-gateway`, `registration-service`, `email-service`, `ai-tutor-service`, `vocabulary-service`, `keyboard-service` and `worksheet-import-service` all validated and reported `0 pending changesets`; no schema update was executed.

ArgoCD switched `playsay-prod-root` to `release/01.007.07` and synchronized `web-app`. All 19 ArgoCD applications reached `Synced/Healthy`; all 12 production pods were ready. The web deployment reported one ready, updated and available replica with the expected immutable digest, release branch and source commit. Both public web origins and API health endpoints returned HTTP `200`. Browser smoke loaded the Honey School sign-in page on both origins with no console errors.

Authenticated teacher/student acceptance remains open because no production account or authenticated browser session was available. It must still confirm an existing game, both supplied games, the LiveWorksheets `710637` material, direct lesson launch, retry after a failed asset request, and the semantic over-limit error. This missing user canary does not invalidate the confirmed edge transport recovery, but production acceptance must not be represented as complete until it passes.

Rollback is GitOps-only for the frontend: return `playsay-prod-root` and `web-app` to `release/01.007.06`. For an edge regression, reapply the prior reviewed Ansible route configuration and validate nginx before reload. There are no database changes to restore.
