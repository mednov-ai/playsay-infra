# Production hotfix release 01.005.03

On 2026-08-30, Jenkins release dispatcher 43 accepted platform commit `2a3987a6556fb56cbc13fbaa7a691c8fdbc0329b`. API build 137 and web build 263 succeeded, and infra candidate `0da1a441d63af3c4b2913d253c2bb8ee1c4f2e59` reached `status: ready`. The candidate pins API digest `sha256:3ee83931f602fdfcf3965005b6b3948254f182742054fdef8a127e227bf28250` and web digest `sha256:20e30ab62e4870d5a6a14aba113de88f95816393d15d68393a0c3cf256bde34e`; the other 16 production image/build blocks matched `release/01.005.02`.

Before migration, `/var/backups/playsay/release-01.005.03-20260830T200839Z` was created on `playsay-prod`. Application, keyboard, worksheet-import and Keycloak custom archives passed SHA-256 and `pg_restore -l` validation with 541, 84, 42 and 536 archive entries respectively.

The bounded operator-only Liquibase Job compared production against the exact reviewed inventory, applied only `2026-08-30-001-add-compact-lesson-access`, and reported zero pending changesets afterward. The additive nullable `lesson_access_link.alias_hash` and `lesson_entry_attempt.request_origin` columns were present before the compatible application rollout; temporary migration Jobs and ConfigMaps were removed.

The production root and all 18 child applications reached `Synced/Healthy` at candidate revision `0da1a441d63af3c4b2913d253c2bb8ee1c4f2e59`. API, web and Keycloak pods were ready with zero restarts; API and web runtime image IDs matched the candidate digests.

Headless Chromium acceptance on both `https://online.honeyschool.ru` and `https://online.honey.school` observed a public `404` for a disposable invalid compact alias rather than authentication rejection, cleared the alias fragment, retained the exact origin in the OIDC callback, and returned the keyboard-focused forgot-password link to the same origin with no page errors or mobile horizontal overflow. A bounded reset canary for the explicitly reported student account recorded registration outcome `CODE_SENT` and one privacy-safe email ledger state `SENT / MAILJET_API / DELIVERED / sent`. This proves provider acceptance, not inbox placement or completion by the account owner.

On 2026-08-31, retained acceptance used disposable role identities without recording credentials. The full registration and password-reset journey passed on dev and separately on both production origins through email receipt, password replacement, old-password rejection, new-password sign-in and cleanup. The authenticated lesson journey passed on dev and production for compact and legacy entry, both production origins, repeated starts, Lobby approval, remembered entry, kick/readmit, foreign-origin denial, rotation/revocation and unchanged Keycloak credential metadata. All temporary lessons, mailboxes and identities were removed.

That production acceptance exposed a remaining Selectel RF-edge defect: response substitution also modified `application/json`, so an API response requested through `.ru` rewrote its intentional `.school` lesson-link alternative back to `.ru`. Infra release `release/01.005.04` at `8552b2682c5f46d37da4dac48f22067bb8eb5750` removed JSON from the substitution types while retaining static HTML/JavaScript/CSS rewriting. Check mode showed only that nginx change; production apply passed `nginx -t` and reloaded nginx. A repeated production lesson journey then returned distinct `.ru` and `.school` links with one alias and passed every retained lesson check.

Bounded reset canaries for the assigned incident teacher and reported student each returned API `202` and reached the privacy-safe ledger state `SENT / MAILJET_API / DELIVERED / sent`; inbox placement and password completion remain with the owners. The legacy incident token disclosed in the report was never replayed. The incident link was rotated through the authorized application endpoint with a disposable `ADMIN` identity: authoritative revision 1 became inactive and revision 2 became active, with both origin variants present. The disposable administrator was deleted and the new raw compact link was not retained in evidence or chat; the assigned teacher obtains it from the application UI. Rollback returns the application root to `release/01.005.02` for the application hotfix and reapplies RF-edge release `release/01.005.03` for the edge-only change; the additive nullable columns remain, and link issuance should be disabled first if lesson entry regresses.
