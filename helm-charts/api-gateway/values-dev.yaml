image:
  repository: ghcr.io/mednov-ai/playsay-api-gateway
  tag: "api-dev-76"
  digest: sha256:20ba84a32191f31b5a867faa2fa74e74505c955b0c811a4d26ae226308edd993
imagePullSecrets:
  - name: ghcr-pull-secret
build:
  name: api-dev-76
  number: "76"
  branch: develop
  branchLabel: api-dev
  commit: b65e40f4b1a2097dc291c9529669889b740351b5
  commitShort: "b65e40f4b1a2"
updateStrategy:
  type: RollingUpdate
  rollingUpdate:
    maxSurge: 0
    maxUnavailable: 1
ingress:
  enabled: false
  host: api-gateway.dev.example.com
auth:
  issuerUri: https://dev.ops.honey.school/keycloak/realms/playsay
  jwkSetUri: http://keycloak.keycloak.svc.cluster.local/keycloak/realms/playsay/protocol/openid-connect/certs
ai:
  openai:
    enabled: true
    existingSecret: playsay-openai
    apiKeyKey: api-key
    model: gpt-5.6-sol
    baseUrl: https://api.openai.com/v1
    reasoning:
      materialDraft: high
      answerSuggestion: low
      htmlGameMetadata: low
database:
  existingSecret: playsay-app-db
  liquibaseEnabled: "false"
  hikariMaxPoolSize: "3"
livekit:
  enabled: true
  serverUrl: wss://dev.online.honey.school/livekit
  existingSecret: livekit-keys
  tokenTtlSeconds: "3600"
lessonTranslation:
  enabled: true
  provider: openai
  model: gpt-realtime-translate
  baseUrl: https://api.openai.com/v1
  existingSecret: playsay-openai
  apiKeyKey: api-key
collaboration:
  websocketUrl: wss://dev.online.honey.school/collab/ws
  existingSecret: playsay-collaboration
  tokenTtlSeconds: "900"
storage:
  provider: s3
  s3:
    endpoint: http://minio.storage.svc.cluster.local:9000
    region: us-east-1
    bucket: playsay-material-assets
    existingSecret: playsay-object-storage
    pathStyleAccess: "true"
    createBucket: "true"
mediaService:
  baseUrl: http://media-service.playsay-dev.svc.cluster.local
  existingSecret: playsay-media
  serviceTokenKey: service-token
  cacheRequestTimeoutSeconds: "660"
paymentService:
  baseUrl: http://payment-service.playsay-dev.svc.cluster.local
  existingSecret: playsay-payment
  serviceTokenKey: service-token
registrationService:
  baseUrl: http://registration-service.playsay-dev.svc.cluster.local
  existingSecret: playsay-registration
  serviceTokenKey: service-token
emailService:
  baseUrl: http://email-service.playsay-dev.svc.cluster.local
  existingSecret: playsay-email
  serviceTokenKey: service-token
publicAppUrl: https://dev.online.honey.school
video:
  youtube:
    rfRelay:
      enabled: "true"
      geoCountryHeader: X-PlaySay-Geo-Country
      requireGeoCountry: "false"
    cache:
      enabled: "true"
      pollDelayMs: "5000"
      leaseSeconds: "900"
      retentionDays: "30"
runtime:
  javaToolOptions: "-XX:InitialRAMPercentage=50 -XX:MaxRAMPercentage=75 -XX:+UseStringDeduplication -Djava.security.egd=file:/dev/urandom"
resources:
  requests:
    cpu: 50m
    memory: 384Mi
  limits:
    cpu: "1"
    memory: 768Mi
