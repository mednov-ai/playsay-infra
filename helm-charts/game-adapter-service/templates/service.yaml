apiVersion: v1
kind: Service
metadata:
  name: {{ include "game-adapter-service.fullname" . }}
  labels:
    {{- include "game-adapter-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "game-adapter-service.selectorLabels" . | nindent 4 }}
