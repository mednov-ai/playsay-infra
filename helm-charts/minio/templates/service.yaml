apiVersion: v1
kind: Service
metadata:
  name: {{ include "playsay-minio.fullname" . }}
  labels:
    {{- include "playsay-minio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: api
      port: {{ .Values.service.apiPort }}
      targetPort: api
      protocol: TCP
    - name: console
      port: {{ .Values.service.consolePort }}
      targetPort: console
      protocol: TCP
  selector:
    {{- include "playsay-minio.selectorLabels" . | nindent 4 }}
