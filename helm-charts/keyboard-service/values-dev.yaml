image:
  repository: ghcr.io/mednov-ai/playsay-keyboard-service
  tag: "key-backend-dev-8"
imagePullSecrets:
  - name: ghcr-pull-secret
build:
  name: key-backend-dev-8
  number: "8"
  branch: develop
  branchLabel: key-backend-dev
  commit: f16b872140becfb9e0d2d792c899a2ce8978e299
  commitShort: f16b872140be
updateStrategy:
  type: RollingUpdate
  rollingUpdate:
    maxSurge: 0
    maxUnavailable: 1
service:
  type: ClusterIP
database:
  existingSecret: playsay-keyboard-db
  liquibaseEnabled: "false"
runtime:
  javaToolOptions: "-XX:InitialRAMPercentage=50 -XX:MaxRAMPercentage=75 -XX:+UseStringDeduplication -Djava.security.egd=file:/dev/urandom"
