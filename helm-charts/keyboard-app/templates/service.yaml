apiVersion: v1
kind: Service
metadata:
  name: {{ include "keyboard-app.fullname" . }}
  labels:
    {{- include "keyboard-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      {{- if and (eq .Values.service.type "NodePort") .Values.service.nodePort }}
      nodePort: {{ .Values.service.nodePort }}
      {{- end }}
  selector:
    {{- include "keyboard-app.selectorLabels" . | nindent 4 }}
