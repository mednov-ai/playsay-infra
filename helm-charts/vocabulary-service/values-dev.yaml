image:
  repository: ghcr.io/mednov-ai/playsay-vocabulary-service
  tag: "vocabulary-codex-hotfix-user-deletion-dev-52"
  digest: sha256:e9b51da38f03811362f54780c53fbea7264cb6f0abf0de1d861e2d0febae7138
imagePullSecrets:
  - name: ghcr-pull-secret
build: {name: vocabulary-codex-hotfix-user-deletion-dev-52, number: "52", branch: codex/hotfix-user-deletion-dev, branchLabel: vocabulary-codex-hotfix-user-deletion-dev, commit: 62ebc4105d13649071976df45cd0e3d3a730eadb, commitShort: 62ebc4105d13}
service: {type: ClusterIP}
updateStrategy:
  type: RollingUpdate
  rollingUpdate: {maxSurge: 0, maxUnavailable: 1}
auth:
  issuerUri: https://dev.ops.honey.school/keycloak/realms/playsay
  jwkSetUri: http://keycloak.keycloak.svc.cluster.local/keycloak/realms/playsay/protocol/openid-connect/certs
database:
  existingSecret: playsay-app-db
  liquibaseEnabled: "false"
  hikariMaxPoolSize: "3"
websocket:
  allowedOriginPatterns: "https://dev.online.honey.school"
openai:
  enabled: true
  existingSecret: playsay-openai
  apiKeyKey: api-key
  baseUrl: https://api.openai.com/v1
  model: gpt-5.6-sol
  reasoningEffort: low
assignmentIntegration:
  gatewayBaseUrl: http://api-gateway.playsay-dev.svc.cluster.local
features:
  composerEnabled: "true"
  adaptivePolicyEnabled: "true"
  deliveryPoliciesEnabled: "true"
  keyNgramsEnabled: "true"
  generatedMediaEnabled: "true"
  lexicalBackfillEnabled: "true"
media:
  generator: openai
  existingSecret: playsay-openai
  apiKeyKey: api-key
  baseUrl: https://api.openai.com/v1
  model: gpt-image-1-mini
  generationWorkerMs: "10000"
  regenerationLimitPerHour: "5"
  reconciliationMs: "60000"
  storage:
    provider: s3
    endpoint: http://minio.storage.svc.cluster.local:9000
    region: us-east-1
    bucket: playsay-vocabulary-media
    existingSecret: playsay-object-storage
    accessKeyKey: access-key
    secretKeyKey: secret-key
    createBucket: "true"
runtime:
  javaToolOptions: "-Xms128m -Xmx256m -XX:+UseStringDeduplication -Djava.security.egd=file:/dev/urandom"
resources:
  requests: {cpu: 25m, memory: 384Mi}
  limits: {cpu: 500m, memory: 512Mi}
